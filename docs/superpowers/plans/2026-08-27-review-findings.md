# Basaar Review Findings

## Baseline (pre-review)

Baseline quality gates recorded on branch `audit/project-review-docs-graphify` at HEAD `e0840d3` (full SHA `e0840d31258335b155b2decb74581e8520920b52`), before any review fixes.

| Gate | Command | Result | Notes |
| --- | --- | --- | --- |
| lint | `pnpm lint` | PASS | `eslint .` exited 0, no warnings or errors |
| typecheck | `pnpm typecheck` | PASS | `tsc --noEmit` exited 0, no errors |
| test | `pnpm test` | PASS | vitest 4.1.9: 4 test files / 22 tests passed (2.46s) |
| build | `pnpm build` | PASS | Next.js 16.2.10 (webpack): prebuild wrote 13 chapters to `public/search-data.json` (1712 KB); serwist bundled `/sw.js`; 17 static pages generated; static export to `out/` |

## Findings (pre-fix)

Static-export Next.js PWA (Arabic book app). Review lenses: dead code/auth remnants+dep usage, unsafe types/casts, error handling, RTL/a11y, PWA-under-static-export incl. build hygiene, naming/pattern consistency, test coverage. No code was changed in this task. Files in scope: `app/` (5), `components/` (11), `lib/` (4 + 4 test files), `scripts/build-search-index.mjs`, `sw.ts`, `next.config.ts`, `eslint.config.mjs`, `tsconfig.json`, `package.json`, `.gitignore`, plus `e2e/` (6 specs) for lens 7.

### Severity-ranked findings

| severity | file:line | finding | suggested fix |
| --- | --- | --- | --- |
| major [fixed] | `app/chapter/[slug]/page.tsx:60` (with `app/globals.css:1`, `package.json:17-28`) | (fixed) The `prose` stack is inert: `@tailwindcss/typography` is neither a dependency (`node_modules/@tailwindcss/` contains only `postcss`) nor loaded via `@plugin`, so `prose`, `prose-lg`, `dark:prose-invert`, `prose-p:...`, `prose-headings:...` compile to nothing. Verified in `out/_next/static/css/*.css`: no typography-plugin rules exist — only the hand-written `.prose h1/h2/h3/p/blockquote` rules (`globals.css:65-91`). Real impact (verified against exported HTML in `out/chapter/*/index.html`): (a) the `prose-lg` / `dark:prose-invert` typographic scale is dead — article elements are styled only by the hand-written `.prose` rules (base fonts, heading colors); (b) markdown lists would render without any list markers (preflight `list-style: none`, no `ul`/`ol`/`li` rules). Note the chapter source currently has no valid MD list syntax — its 171 numbered items are `N-`-prefixed paragraphs (e.g. `content/chapters/chapter-1.md:569`), and rendered articles contain only `<p>` and `<img>` (plus one autolinked URL in `chapter-3.md`), so no lists actually render today; the list-marker gap is latent. There are no in-article markdown links, tables, or code spans in the content. | Install the plugin (`pnpm add @tailwindcss/typography`) and add `@plugin "@tailwindcss/typography";` to `globals.css:1`; or drop the `prose*` classes and style the markdown elements explicitly like the existing `.prose` rules do (and convert the `N-`-paragraph numbering to real lists if lists are intended). |
| major | `components/SearchDialog.tsx:128-131` | Keyboard-open is broken: pressing Enter on a highlighted result only calls `onClose()`; the selected result is never activated/navigated, while the hint bar (`.tsx:209-212` "⏎ فتح") advertises Enter as "open". Keyboard users land back on the trigger with the dialog just closed. | On Enter, `router.push`/use the selected link's href (e.g. `window.location` or a link ref click) before `onClose()`. |
| major | `next.config.ts:6` (`swDest: "public/sw.js"`) + `.gitignore` + `public/sw.js` (tracked, last commit `a4ff094`) | Build hygiene / stale-precache hazard: serwist regenerates `public/sw.js` on every production build (precache manifest embeds per-build hashed `/_next/static/...` URLs). It is a committed artifact, so every build dirts the tree (`M public/sw.js`, confirmed earlier and restored via `git checkout`), and a committed copy can reference asset hashes that don't exist in a fresh checkout unless the deploy pipeline re-runs the build. | Gitignore `public/sw.js` (generated output, like `/out/`) and rely on the deploy-time `pnpm build` (prebuild+build already runs on CI/local). Note `public/search-data.json` is deterministic and tracked — no action. |
| minor | `components/Footer.tsx:8` | Dead Tailwind utilities: `text-muted-foreground` / `text-foreground` are not generated (the tokens are `--muted-foreground`/`--foreground` CSS vars, not Tailwind v4 `--color-*` theme values), so the footer link renders at inherited foreground color with no hover change. Verified: zero matches in compiled CSS. Every other component correctly uses `text-[var(--muted-foreground)]`. | Use `text-[var(--muted-foreground)] hover:text-[var(--foreground)]` to match the codebase pattern. |
| minor | `package.json:27` | `sharp` is an unused direct dependency: `rg` finds zero imports and under `output: "export"` + `images.unoptimized: true` (`next.config.ts:14`) there is no image-optimization pipeline to consume it. | Remove `sharp` from `dependencies` (and prune the lockfile). |
| minor | `package.json:9` | Broken script under static export: `start: "next start"` is unusable with `output: "export"`; running it fails with `"next start" does not work with "output: export" configuration. Use "npx serve@latest out" instead.` (verified). | Replace with `"start": "serve out"` (add `serve` devDep) or drop the script. |
| minor | `supabase/migrations/001_create_bookmarks.sql`, `docs/supabase-setup.md`, `README.md:3,15-16,38,70,78-80,83,85`, `docs/README.md:5`, `docs/upgrade-nextjs.md:13,70`, `docs/SENIOR_INTERVIEW_QUESTIONS.md` | Stale auth/Supabase remnants after commit `5f0ced6`: source code (`app/`, `components/`, `lib/`, `scripts/`) has **zero** Supabase references (verified), but the repo still tracks a `supabase/` migrations dir and multiple docs describing the removed Supabase auth model. In `README.md`: the Supabase badge (`:3`), bookmarks/auth bullets (`:15-16`), "Auth & Storage" (`:38`), the `docs/supabase-setup.md` pointer (`:70`), and the project-tree entries (`:78` "auth callback", `:79` "AuthButton", `:80` "Supabase clients", `:83` "supabase/ migrations", `:85` "supabase-setup" docs row). | Delete `supabase/` and `docs/supabase-setup.md`, scrub README/docs (already itemized in the review plan's doc tasks). |
| note | `.env.local:6-9` (untracked, gitignored per `.gitignore:16`) | Stale auth env vars: live `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` values still sit at `.env.local:8-9` (lines 6-7 are commented-out placeholders). Since `.env*.local` is gitignored the values never ship, and source code has zero reads of either var (verified), so impact is nil today. | Delete the two stale vars (and the commented block) — nothing reads them after the auth removal. |
| minor | `app/sitemap.ts:6` | Stale default base URL: `https://bassaer.vercel.app` points at the retired Vercel deployment (the app moved to Hostinger static hosting). `NEXT_PUBLIC_BASE_URL` is not set anywhere in the repo, so the generated `sitemap.xml` ships Vercel-domain URLs. | Set `NEXT_PUBLIC_BASE_URL` at build/deploy, or update the default to the real domain. |
| minor | `components/SearchDialog.tsx:66-68,170-174` | Fail-open with a misleading indefinite loading state: if `fetch("/search-data.json")` or index construction throws, `isReady` stays `false` and any query shows "جاري تحميل فهرس البحث..." forever (no error UI, no retry). | Add an `error` state; on failure show "تعذر تحميل فهرس البحث" with a retry action (or don't render the loading block at all pre-ready). |
| minor | `components/BookmarkedChapters.tsx:49` | Remove-bookmark button is `opacity-0 group-hover:opacity-100` only: invisible on touch (mobile menu) and to keyboard focus — no `focus-visible` fallback; the control is effectively undiscoverable on phones. | Add `focus-visible:opacity-100` (and consider touch-visible styling on mobile). |
| minor | `components/MobileMenu.tsx:26-62` | A11y of the drawer: it is always in the DOM and remains tabbable/announceable while closed (`translate-x-full` off-screen, `role="dialog" aria-modal="true"` even when hidden, no `aria-hidden`/`inert`); no focus trap or `Escape` handling, and focus is not moved into/restored from the dialog. | Toggle `aria-hidden`/`inert` when closed (or unmount), add `Escape` to close, and move focus into the dialog on open. |
| minor | `components/SearchDialog.tsx:149-160` | The search input has no accessible name (placeholder-only) and the arrow-key selection is not exposed: no `role="combobox"`/`listbox` wiring or `aria-activedescendant`, so AT users get no feedback on the highlighted result. | Add `aria-label="بحث في الكتاب"` to the input and expose the results list with `aria-activedescendant` per selected result. |
| minor | `e2e/pwa.spec.ts:12-15`, `e2e/search.spec.ts:19`, missing coverage | Lens-7 gaps: (a) the "service worker is registered" test only asserts `"serviceWorker" in navigator` (feature detection, passes even if the SW never registers); (b) `search.spec.ts:19` uses `waitForTimeout(1000)` (flaky hard wait during index build); (c) no e2e/interaction coverage for bookmarks (toggle → sidebar refresh via `bookmarks-updated`), reading-progress restore, or InstallPrompt. | Assert `navigator.serviceWorker.getRegistration()` in pwa.spec; poll/wait for index-ready instead of a fixed sleep; add bookmark + progress e2e specs. |
| note | `components/SearchDialog.tsx:11,58`, `components/InstallPrompt.tsx:25`, `sw.ts:6`, `components/ReadingProgressBar.tsx:51` | Lens-2: targeted type workarounds rather than `any` — `as unknown as SearchIndex` (FlexSearch Document), `e as BeforeInstallPromptEvent` (non-standard event), `(self as unknown as {__SW_MANIFEST})` (bundler injection, required by serwist), `parent!` (rAF closure narrowing). Low risk; no implicit `any` found under `strict`. | Optional: replace `parent!` with a captured local; the rest are inherent to the libs — document with comments. |
| note | `lib/bookmarks.ts:24-45` vs `lib/readingProgress.ts:9-37,19-21` | Pattern inconsistency (lens 6): bookmarks exports are `async` wrappers over synchronous localStorage ops (forcing `.then` in callers), while sibling `readingProgress` is sync; also `setLocalProgress` (`readingProgress.ts:19-21`) lacks the `typeof window` guard that both `readStorage`/`writeStorage` have, so calling `saveChapterProgress` in a non-browser context throws `ReferenceError`. | Make bookmarks sync (mirroring readingProgress) or keep async deliberately; add the window guard to `setLocalProgress`. |
| note | `lib/search.ts:19` | `extractExcerpt` uses a case-sensitive `indexOf`; FlexSearch's default tokenizer lowercases, so a Latin/Cyrillic term would be found by the index but the excerpt falls back to the first 150 chars. Arabic book content makes this mostly moot. | Normalize both sides with `toLocaleLowerCase()` before matching. |
| note | `lib/contentLoader.ts:39` | Excerpt always appends `…` even when the first paragraph is ≤ 100 chars. Cosmetic. | Append `…` only when truncated (`p.length > 100`). |
| note | `.gitignore:57-58` | Blanket `*.sh` / `*.bash` ignore is odd: no shell scripts exist today (the build is Node-only) and the pattern can silently skip future harness scripts. | Remove or narrow the pattern. |

### Gate baseline cross-ref

All findings above are observations from the unchanged tree at HEAD `e0840d3` — baseline gates (lint PASS, typecheck PASS, 22/22 vitest PASS, build PASS / 17 static pages) remain green and none of these findings were introduced by the review. No fix is applied in this task; blocker/major findings above are candidates for the follow-up fix task.

### Reviewed files

- `app/`: `page.tsx`, `layout.tsx`, `not-found.tsx`, `sitemap.ts`, `chapter/[slug]/page.tsx` (+ `globals.css` read for token/prose verification)
- `components/`: `ClientShell.tsx`, `Navbar.tsx`, `Sidebar.tsx`, `MobileMenu.tsx`, `SearchDialog.tsx`, `BookmarkButton.tsx`, `BookmarkedChapters.tsx`, `ReadingProgressBar.tsx`, `ErrorBoundary.tsx`, `Footer.tsx`, `InstallPrompt.tsx`
- `lib/`: `contentLoader.ts`, `bookmarks.ts`, `readingProgress.ts`, `search.ts`, `__tests__/` (4 files)
- `scripts/build-search-index.mjs`, `sw.ts`, `next.config.ts`, `eslint.config.mjs`, `tsconfig.json`, `package.json`, `.gitignore`, `public/manifest.json`
- `e2e/`: home, chapter, navigation, search, theme, pwa (6 specs)